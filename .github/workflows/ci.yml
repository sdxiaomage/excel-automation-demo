name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Test zero-dependency CSV tool
        run: npm test
      - name: Rebuild committed CSV demo
        run: npm run demo:csv
      - name: Verify deterministic deliverables
        run: git diff --exit-code -- deliverables/cli
