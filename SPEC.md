# 固定范围验收规格

## 输入

- 1–5 个固定模板 `.xlsx` 或 UTF-8 `.csv` 文件。
- 总计不超过 50,000 行、50 列或 50 MB。
- 第一行为唯一字段名，不含密码、宏、扫描图片或复杂合并单元格。
- 客户提供脱敏样本、期望输出、字段映射及不超过 5 条书面规则。

## 本 Demo 的规则

1. 合并两个订单文件，保留来源文件和原行号。
2. 按 `order_id` 删除完全重复订单；重复记录进入异常表。
3. 日期统一为 `yyyy-mm-dd`。
4. `customer_id` 左连接客户主数据；缺失或无法匹配进入异常表。
5. 数量必须大于 0，单价不得为负，折扣必须在 0–1 之间。
6. 状态统一为 `Paid`、`Pending`、`Refund`。
7. 净额由工作簿公式 `Quantity × Unit Price × (1 − Discount)` 计算。

## 输出与验收

- `Dashboard`：输入、有效、异常、重复、净额和闭环差额。
- `Cleaned Orders`：标准化结果及可审计公式。
- `Exceptions`：来源文件、原行号、字段、错误类型和原因。
- `Run Notes`：规则版本、输入文件和数据处理边界。
- 必须满足：`输入行数 = 有效行数 + 异常行数`，闭环差额为 0。
- 相同输入重复运行，输出数据一致；原始文件不覆盖。
- 全程异步文字沟通，不拉群、不开会、不议价。
- 固定范围内最多接受一次合并后的文字反馈；修订提交后等待审核与付款。

## 固定价格

- ¥399：单文件、≤10,000 行、≤3 条规则、输出 Excel、7 天内一次约定规则缺陷修订。
- ¥700：≤5 文件、≤50,000 行、≤5 条规则、异常报告、7 天内一次约定规则缺陷修订。
- OCR、PDF、宏、数据库/API、定时任务、模板重构或超过 50,000 行另行报价。
