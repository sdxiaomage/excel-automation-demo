{
  "name": "excel-csv-cleanup-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency CSV cleanup, validation, exception reporting, and reconciliation demo",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "csv-cleanroom": "src/csv-cleanroom.mjs"
  },
  "scripts": {
    "test": "node --test",
    "demo:csv": "node src/csv-cleanroom.mjs --config sample_config.json --output deliverables/cli"
  }
}
